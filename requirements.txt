# Core execution + analysis
pandas>=2.0
numpy>=1.24
plotly>=5.18
jupyter>=1.0
ipykernel>=6.25
pyyaml>=6.0
tabulate>=0.9

# Validation (optional but used by src.validation when available)
pandera>=0.18
pydantic>=2.5

# Ontology / graph prototypes
networkx>=3.1

# Modelling (reserved for later segmentation work)
scikit-learn>=1.3

# Testing
pytest>=7.4
